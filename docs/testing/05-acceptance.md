# 测试 05 — 验收标准与发布门禁

## 1. 通用发布门禁（每个版本）

- [ ] CI 全绿：unit + interop + contract + integration（Mock 上游）
- [ ] 覆盖率：`app/` ≥ 85%，关键纯逻辑模块 ≥ 95%，classify/pool 分支 ≥ 90%
- [ ] ruff / mypy-basic 0 error
- [ ] E2E 套件通过（compose 全栈）
- [ ] 真实上游冒烟通过（只读清单，见测试文档 04 §4）
- [ ] 变更涉及加密/封包 → 对拍输出附 PR（测试文档 03 §4）
- [ ] 变更涉及上游分类/池行为 → 对应 CLASSIFY-*/POOL-* 用例先行更新（行为变更与用例变更同 PR）
- [ ] `.env` / `data/` / 向量口令不入镜像与仓库（CI 断言 + 人工复核）
- [ ] 文档同步：02 移植映射勾选、05 协议如有实测修订、CHANGELOG 条目

## 2. 里程碑验收（对应开发文档 07）

### M1 账号运营层

| 验收项 | 标准 |
|--------|------|
| 池化网关 | 3 账号池在注入矩阵（402/429/401/验证码）下服务连续性 100%（INT-002/003/004）；单请求内换号 ≤ max_attempts |
| 额度面板 | PlanSlot 与真实上游 `billing/balance` 字段一一对应；UI 重置倒计时误差 < 1min |
| 领取 | Mock 全退避路径（CL-001~006）通过；真实上游手动领取成功 1 次（含验证码） |
| .zsb 互通 | 与 zcode-switch 双向导入导出成功（BN-005）；负向向量全拒 |
| 安全 | DB raw 扫描无明文凭证（ST-004）；管理面全鉴权 |

### M2 网关扩展

| 验收项 | 标准 |
|--------|------|
| 三协议 | Claude Code / OpenAI SDK / Codex CLI 真实客户端各完成一次完整对话（含流式） |
| 翻译层 | usage/tool_calls 映射用例全绿；SSE 随机分块重组一致（GW-013） |
| 回归 | Anthropic 透传路径行为与 M1 完全一致（回归集） |

### M3 客户端融合

| 验收项 | 标准 |
|--------|------|
| enc:v1 | zsw 官方向量解密一致（ZC-001）；secret 派生矩阵全过（ZC-003） |
| 导入 | 在装有 ZCode 桌面端的真机上，从已登录客户端一键入池成功 |
| 切换 | 真机切换身份后 ZCode 客户端以新账号启动；中断注入不丢号（ZC-006/007） |

### M4 风控对抗（触发式）

| 验收项 | 标准 |
|--------|------|
| 触发响应 | `agent/configs` 出现开关后 1 周内对应能力上线 |
| 签名 | Mock 验证握手/签名/旁路梯子（两次 401 VERIFY → unsigned bypass） |
| 兼容 | 开关未开时行为与 M2/M3 完全一致（fail-open） |

## 3. 性能与稳定性基线

- 单实例（2C4G，tebi 规格）：≥ 30 并发流式请求无 5xx（Mock 上游）；内存 RSS < 400MB
- 转发开销：P95 增幅 < 15ms（TTFB 对比直连 Mock）
- 72h 长跑（Mock）：无内存增长（泄漏阈值 < 10%）、无句柄泄漏、池状态无漂移
- 验证码求解缓存命中率（持续负载下）> 90%

## 4. 安全验收清单

- [ ] 后台默认密码首次登录强制修改（或启动时告警）
- [ ] 网关 key 生产部署必须配置（compose/文档提示；无 key 监听非回环地址时启动告警）
- [ ] 日志无完整凭证/verifyParam（log 扫描断言）
- [ ] 导出文件仅在内存组装，不落临时盘
- [ ] 路径穿越用例（ZC-008）与 admin 注入用例通过

## 5. 发布产物

| 产物 | 说明 |
|------|------|
| Docker 镜像（GHCR） | 多架构 amd64/arm64；镜像内含 Node 求解器依赖 |
| docker-compose.yml | 数据卷 `/data`；env 示例齐全 |
| 裸机部署包 | 源码 + requirements 锁定（pip-tools）；tebi supervisor 样例（开发文档 06 §5） |
| CHANGELOG + 协议观测记录 | 上游行为变化、向量更新说明 |

## 6. 回滚

- 镜像版本化 tag，`supervisorctl`/compose 一键回退上一 tag
- SQLite 结构变更必须向后兼容一版（新列可空），回滚不破坏数据
- 验证码 solver.js 更新保留上一版本文件（`solver.v{n}.js`），可 env 切回
